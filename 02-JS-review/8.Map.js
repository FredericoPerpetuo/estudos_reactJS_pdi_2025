/**
 * MAP
 * Percorres todos os elementos de um array aplicando uma função a cada um deles
 */
const nums = [1, 2, 3, 4, 5];

const numsMap = nums.map((element) => element * 5);

console.log(nums);
console.log(numsMap);
