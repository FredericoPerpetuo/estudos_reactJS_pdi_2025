/**
 * ARROW FUNCTIONS
 * Possuem sintaxe mais enxuta
 * Quando possuem uma única linha não  necessitam de return ou {}
 *
 * UTILIDADE
 * Escrever funções curtas e com sintaxe mais enxuta
 */
const soma = (x, y) => x + y;
console.log(soma(10, 20));
